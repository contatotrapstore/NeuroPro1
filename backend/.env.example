# Environment Configuration - NeuroIA Lab Backend
# IMPORTANTE: Copie este arquivo para .env e configure com seus valores reais

# Application Environment (REQUIRED)
NODE_ENV=development
PORT=3000

# Database - Supabase Configuration (REQUIRED)
# SUBSTITUA pelos seus valores reais do Supabase Dashboard > Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-role-key
SUPABASE_ANON_KEY=your-supabase-anon-key

# AI Service - OpenAI Configuration (REQUIRED)
# SUBSTITUA pela sua chave da OpenAI (https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-your-openai-api-key
# OPENAI_ORGANIZATION=org-your-openai-organization-id (opcional)

# Payment Gateway - Asaas Configuration (REQUIRED)
# SUBSTITUA pelas suas credenciais do Asaas (https://www.asaas.com)
ASAAS_API_KEY=your-asaas-api-key
ASAAS_WEBHOOK_SECRET=your-asaas-webhook-secret

# Security & CORS (IMPORTANT)
# Para desenvolvimento local
CORS_ORIGIN=http://localhost:5173
# Para produção: CORS_ORIGIN=https://your-domain.vercel.app
JWT_SECRET=your-super-secure-jwt-secret-key-minimum-32-characters

# Cache (OPTIONAL) - Desabilitado por padrão para evitar erros de conexão
# REDIS_URL=redis://localhost:6379

# Development Settings (OPTIONAL)
DEBUG=false

# ========================
# CONFIGURAÇÃO PARA VERCEL
# ========================
# Para deploy no Vercel, configure as seguintes variáveis no dashboard:
# NODE_ENV=production
# PORT=3000 (gerenciado automaticamente pelo Vercel)
# CORS_ORIGIN=https://seu-dominio.vercel.app
# Todas as outras variáveis acima também devem ser configuradas