# NeuroIA Lab Environment Variables
# Copy this file to .env and fill in your actual values

# ===========================================
# FRONTEND ENVIRONMENT VARIABLES
# ===========================================

# Supabase Configuration (REQUIRED)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# API Base URL (REQUIRED)
# Development: /api (uses Vite proxy)
# Production: https://your-backend.vercel.app
VITE_API_BASE_URL=/api

# Development Settings (OPTIONAL)
VITE_ENVIRONMENT=development
VITE_DEBUG=true

# ===========================================
# BACKEND ENVIRONMENT VARIABLES
# ===========================================

# Application Environment (REQUIRED)
NODE_ENV=development
PORT=3000

# Database - Supabase Configuration (REQUIRED)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-key
SUPABASE_ANON_KEY=your-supabase-anon-key

# AI Service - OpenAI Configuration (REQUIRED)
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_ORGANIZATION=org-your-openai-organization-id

# Payment Gateway - Asaas Configuration (REQUIRED)
ASAAS_API_KEY=your-asaas-api-key
ASAAS_WEBHOOK_SECRET=your-asaas-webhook-secret

# Security & CORS (REQUIRED for JWT)
JWT_SECRET=your-very-secure-jwt-secret-key-minimum-32-characters
CORS_ORIGIN=http://localhost:5173

# Cache (OPTIONAL)
REDIS_URL=redis://localhost:6379

# Development Settings (OPTIONAL)
DEBUG=false

# ===========================================
# VERCEL DEPLOYMENT SETTINGS
# ===========================================

# For Vercel Production Deployment, set these in Vercel Dashboard:
# NODE_ENV=production
# CORS_ORIGIN=https://your-frontend-domain.vercel.app
# All other variables above (except VITE_* which go in frontend project)

# ===========================================
# SETUP INSTRUCTIONS
# ===========================================

# 1. Copy this file to .env in the project root
# 2. Copy frontend section to frontend/.env.local
# 3. Copy backend section to backend/.env
# 4. Copy backend section to api/.env
# 5. Fill in all values with your actual credentials
# 6. Never commit .env files to Git (already in .gitignore)

# ===========================================
# GETTING CREDENTIALS
# ===========================================

# Supabase:
#   1. Create account at supabase.com
#   2. Create new project
#   3. Go to Settings > API
#   4. Copy URL and anon key

# OpenAI:
#   1. Create account at openai.com
#   2. Go to API section
#   3. Create new API key
#   4. Copy the key (starts with sk-)

# Asaas:
#   1. Create account at asaas.com
#   2. Go to API section
#   3. Generate API key and webhook secret

# JWT Secret:
#   Generate a secure random string (32+ characters)
#   Example: openssl rand -base64 32